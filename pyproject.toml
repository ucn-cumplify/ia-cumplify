[project]
name = "ia-cumplify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "towcrash", email = "fernando.vega@alumnos.ucn.cl" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ia-cumplify = "ia_cumplify:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
